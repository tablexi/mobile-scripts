#!/usr/bin/env ruby

platforms = ['ios', 'android', 'react-native']
bin_dir = File.expand_path(File.dirname(__FILE__))

args =  ARGV
args = platforms if args.empty?
puts "Installing platforms: #{args}"

args.each do |p| 
  puts `#{bin_dir}/setup-#{p}` 
end
puts "Setup finished 🚀".green